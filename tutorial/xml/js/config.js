/* ============================================
   EDITABLE CONFIGURATION
   Change these values only — no other code needs to change.
   ============================================ */

// Landing page the "Go to Course Landing Page" button redirects to.
var landingPageURL = "https://uncodemy.com/";

// Counselor contact details shown in the "Need Help?" card.
var phoneNumber = "+91 9818366550";
var whatsappLink = "https://api.whatsapp.com/send?phone=918800023723";

// Upcoming batch details shown in the right sidebar.
var batchCourseName = "XML";
var batchStartDate = "Coming Soon";
var batchDuration = "4 Months";

// Number of days the countdown timer runs for, starting from the
// visitor's first page load (persisted in localStorage so it does
// not reset on refresh).
var countdownDays = 5;
